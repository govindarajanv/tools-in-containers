# DefectDojo

```
docker run -it -p 8000:8000 appsecpipeline/django-defectdojo bash -c "export LOAD_SAMPLE_DATA=True && export DOJO_ADMIN_USER=admin && export DOJO_ADMIN_PASSWORD=admin && bash /opt/django-DefectDojo/docker/docker-startup.bash"
```
